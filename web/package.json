{
  "name": "tts-onnx-web",
  "version": "1.0.0",
  "description": "TTS inference using ONNX Runtime for Web Browser",
  "type": "module",
  "scripts": {
    "prepare": "node prepare-assets.js",
    "dev": "vite",
    "build": "npm run prepare && vite build && node post-build.js && node verify-build.js",
    "preview": "vite preview",
    "verify": "node verify-build.js",
    "postbuild": "node post-build.js"
  },
  "keywords": ["tts", "onnx", "speech-synthesis", "web"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "fft.js": "^4.0.3",
    "onnxruntime-web": "^1.17.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
